<!-- xxx.hml -->
<div class="container">
    <text class="title">
        {{urlParams.name || 'EOS'}} 7D价格趋势
    </text>
    <stack class="chart-region">
        <chart class="chart-data" type="line" ref="linechart" options="{{lineOps}}" datasets="{{lineData}}"></chart>
    </stack>
    <button value="设置价格" onclick="toSetPrice"></button>
</div>